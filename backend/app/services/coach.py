class CoachService:
    """Reserved service boundary for future AI coaching workflows."""

    async def generate_daily_recommendations(self, user_id: int) -> None:
        _ = user_id
        return None
